/*
 *	 print the last ten primes below N
*/
#include <iostream>
#include <iomanip>
#include <ranges>

//using std::cout; using std::setw;
//using std::ranges::views::iota; using std::ranges::views::filter;
//using std::ranges::views::transform; using std::ranges::views::reverse;
//using std::ranges::views::take;

//constexpr int N{ 1000 };
//bool is_prime(const int number);
//void nl(int n = 1) { for (size_t i{ 0 }; i < n; ++i) cout << "\n"; }

int main() {
	cout << "\n\n\t hello \n\n";
//    nl();
//    for (auto n : iota(1, N) | reverse | filter(is_prime) | take(10) | reverse)
//        cout << setw(8) << n;
//    nl(2);
//    return 0;
}


//bool is_prime(const int number) {
//	if (number < 2) return false;
//	for (int i = 2; i * i <= number; i++)
//		if (number % i == 0) return false;
//	return true;
//}
